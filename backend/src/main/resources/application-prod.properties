# Production Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ques-bank?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true
spring.datasource.username=gcpadmin
spring.datasource.password=r451sRA#sqw2hQ
# Explicitly set driver — required for Tomcat WAR deployment (auto-detect sometimes fails)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# File Upload Configuration
spring.servlet.multipart.max-file-size=2048MB
spring.servlet.multipart.max-request-size=2048MB
file.upload.dir=uploads


# HikariCP Connection Pool (Production Optimized)
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=3000
spring.datasource.hikari.validation-timeout=2000
spring.datasource.hikari.leak-detection-threshold=30000

# Production JPA Settings & Performance
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=true

# ── CORS Allowed Origins (Production) ────────────────────────────────────────
# To change domain: just update this line — no code changes needed!
# Add comma-separated origins (https AND http versions)
app.cors.allowed-origins=https://qb.learningshaper.com,http://qb.learningshaper.com,http://localhost:5173,http://192.168.*:*,http://10.*:*,http://172.*:*,http://localhost:*

# ── Pinecone Vector DB Settings (Production) ─────────────────────────────────
app.pinecone.api-key=pcsk_P2WWH_7bbZaFgrz2Fn1hoAYDug8uS194ujyGvNyg6aoqQyekYEWF3NooZV4Rgc9HzwjUJ
app.pinecone.index-name=questionshaper-index
app.pinecone.dimension=768
app.pinecone.host=https://questionshaper-index-j93p4ul.svc.aped-4627-b74a.pinecone.io

# Redis Production Configuration
spring.data.redis.host=127.0.0.1
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=32
spring.data.redis.lettuce.pool.max-idle=16
spring.data.redis.lettuce.pool.min-idle=4
spring.data.redis.lettuce.pool.max-wait=1000ms
app.redis.enabled=true

# Meilisearch Production Configuration
app.meilisearch.host=http://127.0.0.1:7700
app.meilisearch.api-key=masterKey
app.meilisearch.enabled=true

# API Security & Rate Limiting Production Configuration
app.ratelimit.enabled=true
app.ratelimit.public-limit=120
app.ratelimit.auth-limit=300

# Spring Boot Actuator Production Monitoring
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when_authorized
management.health.db.enabled=true
management.health.redis.enabled=true
